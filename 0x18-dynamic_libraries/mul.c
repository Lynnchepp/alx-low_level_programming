#include <stdlib.h>

/**
 * mul - multiplies two intergers
 * @a: first integer
 * @b: second interger
 * Return: the multiplication result of the two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
