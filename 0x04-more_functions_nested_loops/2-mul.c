#include "main.h"
/**
 * mul -multiplies two integers
 * @a -first integer to be multiplied
 * @b -second iteger to be multiplied
 * retur: the result of multiplying a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
